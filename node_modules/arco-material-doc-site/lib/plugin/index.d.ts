import { ModuleUMDInfo } from '@arco-materials/material-preview-utils/es/interface';
import { ModuleExportMap } from './parseModuleExport';
export declare type ModuleInfo = {
    name: string;
    info: Record<string, string> & {
        umd: ModuleUMDInfo;
    };
    isDoc?: boolean;
    children?: Array<{
        name: string;
        info: Record<string, string>;
        rawCode?: string;
    }>;
};
declare type ChunkInfo = {
    name: string;
    entry: string;
    files?: string[];
};
export default class ArcoSiteModuleInfoPlugin {
    private readonly options;
    private paths;
    constructor(options: any);
    parseModuleExport({ context, filePathList, statsModules, needRawCode }: {
        context: any;
        filePathList: any;
        statsModules: any;
        needRawCode?: boolean;
    }): ModuleExportMap;
    getModuleInfoMap({ chunkInfoList, moduleExportMap, }: {
        moduleExportMap: ModuleExportMap;
        chunkInfoList: Array<ChunkInfo>;
    }): {
        /**
         * Module info like { zh-CN: { name: 'Button', info: {}, children: [] } }
         */
        [key: string]: ModuleInfo;
    };
    apply(compiler: any): void;
}
export {};
